Variables (una variable se usa para almacenar el nombre de algo que usaremos mas tarde );
Strings ( una "strings" es una cadena de texto que siempre van entre comillas );
Funciones (argumentos, return):(una funcion es una porcion de codigo que hacen una tarea en concreto);
(un argumento sirve para darle nombre a una funcion y asi poder invocarla);
(el retorn es para que nos devuelva un valor y finalice una funcion);
Declaraciones if(un if se utiliza para evialuar una exprecion condicional, si se cumple devuelve verdadero y si es falsa ejecuta otra sentencia, en este caso seria false);
Valores booleanos (true, false)(los valores booleanos son tipo de dato logico que representa logica binaria y devuelve solamente true(verdadero) o false (falso));